version: '3.8'
services:
  web:
    build: .
    ports:
      - "80:80"
    container_name: pro192_web
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    # Uncomment if you want to mount local files for development
    # volumes:
    #   - ./src:/app/src
    #   - ./public:/app/public
    #   - ./Org_code:/app/Org_code
